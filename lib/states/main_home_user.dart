import 'package:flutter/material.dart';

class MainHomeUser extends StatelessWidget {
  const MainHomeUser({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}